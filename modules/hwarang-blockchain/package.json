{
  "name": "hwarang-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "HWARANG Token - 적응형 발행 + 자동 소각 + Proof of Useful Work",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network mumbai",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network polygon",
    "simulate": "ts-node scripts/simulate.ts",
    "seed": "ts-node scripts/seed-initial.ts"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.5",
    "ts-node": "^10.9",
    "@types/node": "^22.0"
  }
}
